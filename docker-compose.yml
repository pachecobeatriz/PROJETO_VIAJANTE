version: '3.8'

services:

  db:
    image: mysql:8.0
    container_name: viajante_mysql
    restart: always
    environment:
      - MYSQL_DATABASE=dbviajante
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=pv
      - MYSQL_PASSWORD=pv
    ports:
      - '4306:3306'
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - viajante_network

  backend:
    build: ./backend
    container_name: viajante_backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/dbviajante
      SPRING_DATASOURCE_USERNAME: pv
      SPRING_DATASOURCE_PASSWORD: pv
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
    depends_on:
      db:
        condition: service_healthy
    networks:
      - viajante_network
  
  frontend:
    build: ./frontend
    container_name: viajante_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - viajante_network

volumes:
  mysql_data:
networks:
  viajante_network:
    driver: bridge
